# Site
name: "TotulRedus"
description: "Reduceri Reduceri Reduceri"
meta_description: "Cele mai mari reduceri la o varietate largă de produse."
logo: 'assets/images/logo.png'
favicon: 'favicon.png'
url: "https://totredus.ro"
google_analytics: 'UA-86031208-5'



    
# Other
markdown: kramdown
highlighter: rouge
permalink: /:categories/:title.html



#defaults for product category and pages
defaults:
  -   
    scope:
      path: "_posts/fashion" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      layout: "category"
      category: "fashion"

  -   
    scope:
      path: "_posts/tehnologie" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      layout: "category"
      category: "tehnologie"
  -   
    scope:
      path: "_posts/incaltaminte" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      layout: "category"
      category: "incaltaminte"	  
  -   
    scope:
      path: "_posts/cosmetice-parfumuri" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      layout: "category"
      category: "cosmetice-parfumuri"	  
